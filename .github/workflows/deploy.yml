name: CD

# Deploys on every push to main. Contracts deploy first (opt-in — needs
# STELLAR_SECRET_KEY, skips by default) but only ever targets Stellar
# TESTNET; its outputs feed deploy-relayer's env vars so the relayer keeps
# pointing at whatever testnet contracts are currently live.
#
# deploy-frontend is separate and targets Stellar MAINNET: it deploys the
# live production frontend (aetherstellar.vercel.app) with hardcoded mainnet
# network config and mainnet contract addresses. It does NOT consume
# deploy-contract's outputs — those are testnet addresses and would be
# actively wrong on mainnet. If the mainnet contracts are ever redeployed,
# update the addresses in the deploy-frontend step by hand.
on:
  push:
    branches: [main]
  workflow_dispatch: {}

# Least-privilege default — these jobs only call external APIs (Stellar RPC,
# Render, Vercel) via secrets, none need to write to the repo itself.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  # ────────────────────── Deploy smart contracts ──────────────────────
  deploy-contract:
    name: Deploy contract (Stellar testnet)
    runs-on: ubuntu-latest
    # References the repo's existing "Production" GitHub Environment (created
    # 2026-06-30, currently has zero protection rules configured). This gives
    # the repo owner a place to add required reviewers/wait timers (Settings
    # → Environments → Production) before any push to main can reach a live
    # deploy — currently a no-op until that's configured; add it now rather
    # than after a mainnet job gets wired into this file under time pressure.
    environment: Production
    outputs:
      zk_verifier: ${{ steps.addresses.outputs.zk_verifier }}
      escrow_vault: ${{ steps.addresses.outputs.escrow_vault }}
      order_book: ${{ steps.addresses.outputs.order_book }}
      settlement: ${{ steps.addresses.outputs.settlement }}
      matching_engine: ${{ steps.addresses.outputs.matching_engine }}
      deployed: ${{ steps.addresses.outputs.deployed }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain (+ wasm32v1-none target)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32v1-none

      - name: Cache Cargo build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: contracts

      # Fallback system libs in case binstall has to compile from source
      # (stellar-cli's hardware-wallet/keyring crates link dbus-1 and libudev).
      - name: Install Stellar CLI build deps
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev libudev-dev pkg-config

      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main

      # Pulls the prebuilt stellar binary — no native compilation needed.
      - name: Install Stellar CLI
        run: cargo binstall --no-confirm stellar-cli

      - name: Deploy and initialize contracts
        id: addresses
        working-directory: contracts
        env:
          STELLAR_SECRET_KEY: ${{ secrets.STELLAR_SECRET_KEY }}
          RELAYER_ADDRESS: ${{ vars.RELAYER_ADDRESS || 'GD7ZTJ6XFHONCB5P52LBXOG2DQUOOKAWXPKTCTJCHI352RCWPKUBMG6Z' }}
        run: |
          set -euo pipefail

          if [ -z "$STELLAR_SECRET_KEY" ]; then
            echo "::warning::STELLAR_SECRET_KEY not set — skipping contract deploy. Add it in repo Settings → Secrets and variables → Actions to enable this job."
            echo "deployed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Import the funded deployer key under the alias scripts/deploy.sh
          # and scripts/initialize.sh expect ("darkpool-deployer").
          printf '%s\n' "$STELLAR_SECRET_KEY" | stellar keys add darkpool-deployer --secret-key --overwrite

          ./scripts/build.sh
          ./scripts/deploy.sh
          ./scripts/initialize.sh

          # deploy.sh writes the 5 fresh addresses into ../.env — surface them
          # as job outputs so deploy-relayer and deploy-frontend below build
          # against what was *actually just deployed*, not a stale value.
          # shellcheck disable=SC1091
          source ../.env
          {
            echo "zk_verifier=$ZK_VERIFIER_ADDRESS"
            echo "escrow_vault=$ESCROW_VAULT_ADDRESS"
            echo "order_book=$ORDER_BOOK_ADDRESS"
            echo "settlement=$SETTLEMENT_ADDRESS"
            echo "matching_engine=$MATCHING_ENGINE_ADDRESS"
            echo "deployed=true"
          } >> "$GITHUB_OUTPUT"

  # ────────────────────────── Deploy relayer ───────────────────────────
  deploy-relayer:
    name: Deploy relayer (Render)
    runs-on: ubuntu-latest
    environment: Production
    needs: [deploy-contract]
    # No dependency on deploy-contract actually deploying — contract deploy
    # is opt-in (needs STELLAR_SECRET_KEY) and skips by default. The relayer
    # still needs to ship on every push; it just doesn't rewrite its env vars
    # unless deploy-contract produced genuinely new addresses this run.
    steps:
      - name: Update Render env vars (if contracts redeployed) and redeploy
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
          CONTRACTS_DEPLOYED: ${{ needs.deploy-contract.outputs.deployed }}
        run: |
          set -euo pipefail

          if [ -z "$RENDER_API_KEY" ] || [ -z "$RENDER_SERVICE_ID" ]; then
            echo "::warning::RENDER_API_KEY / RENDER_SERVICE_ID not set — skipping relayer redeploy. Add both in repo Settings → Secrets and variables → Actions to enable this job."
            exit 0
          fi

          if [ "$CONTRACTS_DEPLOYED" = "true" ]; then
            # PUT .../env-vars/{key} upserts ONE variable — unlike the plural
            # .../env-vars endpoint (full replace), this cannot clobber
            # unrelated relayer config (Mongo URI, batch interval, port, etc).
            set_env_var() {
              curl -sf -X PUT "https://api.render.com/v1/services/$RENDER_SERVICE_ID/env-vars/$1" \
                -H "Authorization: Bearer $RENDER_API_KEY" \
                -H "Content-Type: application/json" \
                -d "{\"value\": \"$2\"}" > /dev/null
              echo "    set $1"
            }

            echo "==> Contracts were redeployed — updating relayer env vars to the new addresses..."
            set_env_var ZK_VERIFIER_ADDRESS     "${{ needs.deploy-contract.outputs.zk_verifier }}"
            set_env_var ESCROW_VAULT_ADDRESS    "${{ needs.deploy-contract.outputs.escrow_vault }}"
            set_env_var ORDER_BOOK_ADDRESS      "${{ needs.deploy-contract.outputs.order_book }}"
            set_env_var SETTLEMENT_ADDRESS      "${{ needs.deploy-contract.outputs.settlement }}"
            set_env_var MATCHING_ENGINE_ADDRESS "${{ needs.deploy-contract.outputs.matching_engine }}"
          else
            echo "==> Contracts were not redeployed this run — leaving relayer env vars as-is."
          fi

          echo "==> Triggering Render deploy..."
          curl -sf -X POST "https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": "do_not_clear"}'

  # ────────────────────────── Deploy frontend ──────────────────────────
  deploy-frontend:
    name: Deploy frontend (Vercel, mainnet)
    runs-on: ubuntu-latest
    environment: Production
    needs: [deploy-relayer]
    # No dependency on deploy-contract's outputs: that job deploys contracts
    # to Stellar TESTNET only (see its name/comments above) and is unrelated
    # to the mainnet contracts this frontend targets. The addresses below are
    # the live mainnet contracts (confirmed deployed/initialized on Stellar
    # public network) — update them here if the mainnet contracts are ever
    # redeployed; nothing wires them automatically the way testnet's are.
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Vercel (production)
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ vars.VERCEL_ORG_ID || 'team_CItqKbVUksF6aywbkwxXgAgI' }}
          VERCEL_PROJECT_ID: ${{ vars.VERCEL_PROJECT_ID || 'prj_8NFJ6bSlKbr5ZewQ9K6PAAiWWXZz' }}
        run: |
          set -euo pipefail

          if [ -z "$VERCEL_TOKEN" ]; then
            echo "::warning::VERCEL_TOKEN not set — skipping Vercel deploy. Add it in repo Settings → Secrets and variables → Actions to enable this job."
            exit 0
          fi

          # .vercel/project.json is gitignored; VERCEL_ORG_ID + VERCEL_PROJECT_ID
          # env vars are Vercel's documented way to skip interactive `vercel link`.
          npx --yes vercel deploy --prod --yes --token "$VERCEL_TOKEN" \
            -b NEXT_PUBLIC_ZK_VERIFIER_ADDRESS=CDW4JQNQKMNVLXJKI5HEOKVD4UFH3GPH2HKXPZZCHVLIGMGGVBSQJHZ7 \
            -b NEXT_PUBLIC_ESCROW_VAULT_ADDRESS=CAJ3A27JEGOQEPAQIDJFER3QBEZQKTZJR6OF4MR726IX3NWTW3QUYWT2 \
            -b NEXT_PUBLIC_ORDER_BOOK_ADDRESS=CAXR5KWD7EGYD5BP5TPIULBN34JGCGEPQU6XCQXPV4DWVQ6IBE6ZOSA5 \
            -b NEXT_PUBLIC_SETTLEMENT_ADDRESS=CAPWWZPALWF4TRUV6ZBHYDFO5BJFVVJDE36WRU4AHKEJ5INB3FUHKB56 \
            -b NEXT_PUBLIC_MATCHING_ENGINE_ADDRESS=CAOTNMFUXGRGA6QACDFVPJ4TFDDFNASWEMBCHPSVBJ3ZQSRWW5XF52H2 \
            -b NEXT_PUBLIC_STELLAR_NETWORK=mainnet \
            -b NEXT_PUBLIC_STELLAR_RPC_URL=https://mainnet.sorobanrpc.com \
            -b NEXT_PUBLIC_STELLAR_HORIZON_URL=https://horizon.stellar.org \
            -b NEXT_PUBLIC_XLM_TOKEN_ADDRESS=native \
            -b NEXT_PUBLIC_USDC_TOKEN_ADDRESS="${{ vars.MAINNET_USDC_TOKEN_ADDRESS }}" \
            -b NEXT_PUBLIC_RELAYER_URL="${{ vars.RELAYER_URL || 'https://aether-w5p8.onrender.com' }}"
